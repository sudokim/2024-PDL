# load_col:
#   inter: [user_id, item_id, item_id_list]

# alias_of_item_id: [item_id, item_id_list]

train_batch_size: 2048
train_neg_sample_args: null

# eval_args:
#   group_by: user
#   order: TO
#   split:
#     LS: valid_and_test
# valid_metric: NDCG@10

metrics: [Recall, NDCG, MRR]
topk: [10, 20, 50]
 
# benchmark_filename: ['train', 'valid', 'test']

# Debug
# epochs: 500
